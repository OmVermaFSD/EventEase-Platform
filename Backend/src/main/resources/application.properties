# Application Configuration
spring.application.name=event-ease-backend
server.port=8080

# Database Configuration (H2 for development)
spring.datasource.url=jdbc:h2:mem:eventease
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

# Data Initialization
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data.sql

# CORS Configuration for Frontend Sync
# Frontend runs on http://localhost:5173 (Vite default)

# Logging Configuration
logging.level.com.eventease=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG

# Redis Configuration (for rate limiting)
# spring.redis.host=localhost
# spring.redis.port=6379

# PostgreSQL Configuration (for production)
# spring.datasource.url=jdbc:postgresql://localhost:5432/eventease
# spring.datasource.username=eventease
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
